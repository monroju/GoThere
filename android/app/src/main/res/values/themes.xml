<resources>
    <!-- Splash screen theme (shown while app cold-starts) -->
    <style name="Theme.GoThere.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#151B23</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">#151B23</item>
        <item name="postSplashScreenTheme">@style/Theme.GoThere</item>
    </style>

    <!-- Main app theme (edge-to-edge handled programmatically) -->
    <style name="Theme.GoThere" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
